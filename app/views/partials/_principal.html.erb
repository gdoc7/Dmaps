  <header>
    <nav class="ngtn">
      <%= link_to image_tag("logodm.png"), root_path  %>
          <ul style="list-style: none">
            <% if user_signed_in? %>
              <li  class="list" >
                <%= link_to "Cerrar Sesion", destroy_user_session_path, method: :delete , :style => "color:white" %>
              </li>
              <li class="list" >
                <%= link_to "Ordenes", orders_path, :style => "color:white"  %>
              </li>
              <%else %>
              <% if current_page?(root_path)%>
              <li class="list" >
              <a href="#footer" style="color: black"><i class="fa fa-fw fa-envelope"></i> Contacto </a>
              </li>
                <%end %>

                <%end%>
          </ul>
    </nav>
  </header>






